plugins { id 'com.android.application' }

android {
    namespace 'com.ronny.erpmaterialesnorte'
    compileSdk 35

    defaultConfig {
        applicationId 'com.ronny.erpmaterialesnorte'
        minSdk 24
        targetSdk 35
        versionCode 6
        versionName '1.5.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
